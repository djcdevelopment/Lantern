# Loaded automatically by `vite build --mode cloud` (= `npm run build:cloud`).
# Cloud mode: GET requests hit pre-rendered static blobs at public/api/*.json
# (same origin), and writes throw CloudUnavailableError. See src/api/http/client.ts.
VITE_CLOUD_MODE=true
